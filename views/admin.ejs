<!DOCTYPE html> 
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Admin Dashboard</title>
   <style>
  body {
  margin: 0;
  padding: 0;
  font-family:Impact, Haettenschweiler, 'Arial Narrow Bold', sans-serif;
  background-image: url("image/back.jpg");
  background-size: cover;
  background-repeat: no-repeat;
  background-position: center;
  background-attachment: fixed;
  height: 100vh;
  overflow: hidden;
}
/* Navigation Bar */
.navigation {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background: linear-gradient(to right,#654ea3,#eaafc8); /* Purple-blue gradient */
  color: black;
  padding: 15px 30px;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
}

.navigation h3 {
  margin: 0 auto;
  font-size: 30px;
  font-weight: bold;
  margin-left: 550px;;
}

/* Profile Section */
.profile-container {
  position: relative;
  cursor: pointer;
}

.profile-icon {
  color: black;
  font-size: 40px;
}

.profile-menu {
  display: none;
  position: absolute;
  top: 45px;
  right: 0;
  background: white;
  color: #333;
  border: 1px solid #ccc;
  border-radius: 8px;
  box-shadow: 0 6px 15px rgba(0, 0, 0, 0.2);
  width: 220px;
  padding: 15px;
  z-index: 1000;
}

.profile-menu p {
  margin: 6px 0;
  font-size: 14px;
}

.profile-menu a {
  text-decoration: none;
  color: #6a1b9a;
  font-weight: bold;
  margin-top: 10px;
  display: block;
}

/* Dashboard Layout */
.dashboard-container {
 display: flex;
      flex-wrap: nowrap;
      height: calc(100vh - 70px);
      overflow: hidden;

}

/* Sidebar */
.sidebar {
  width:100px;
 
}
.sidebar-menu {
  width:230px;
  padding-top: 20px;
  color: #fff;
  position: sticky;
  top: 0;
  height: 100%;
}

.sidebar-menu li {
  padding: 14px 20px;
  font-size: 16px;
  display: flex;
  align-items: center;
  border-bottom: 1px solid white;
  
}

.sidebar-menu li a {
  text-decoration: none;
  color:white;
  display: flex;
  gap: 8px;
  align-items: center;
}
.sidebar-menu li:hover {
    background-color:white;
  
}
.sidebar-menu li:hover a {
  color:purple;
  font-weight: bold;
  
}

/* Main Content Area */
.main-body {
  flex: 1;
  padding: 40px;
  color:black;
  overflow: auto;
  height: 100%;
  margin-top:-10x; 
}
.iframe-wrapper {
  width: 100%;
  height: 100%;
  border-radius: 10px;
  overflow: hidden;
}

.iframe-wrapper iframe {
  width: 100%;
  height: 100%;
  border: none;
}

/* Scrollbar Removal */
.main-body::-webkit-scrollbar {
  width: 0;
  background: transparent;
}


</style>

  <!-- Font Awesome & Fonts -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css"/>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;700&display=swap" rel="stylesheet"/>
  <link rel="stylesheet" href="style.css">
</head>
<body>

  <!-- Top Navigation Bar -->
  <nav class="navigation">
    <div class="navbar-left">
      <h3>Admin Panel</h3>
    </div>
    <div class="navbar-right">
      <div class="profile-container" onclick="toggleProfileMenu()">
        <i class="fa fa-user-circle-o profile-icon" style="font-size:40px;"></i>
        <div class="profile-menu" id="profileMenu">
          <p><strong>Name:</strong>Madhuri</p>
          <p><strong>Email:</strong> madhuri@gmail.com</p>
          <p><strong>Role:</strong> Admin</p>
          <!-- <a href="logout">Logout</a> -->
        </div>
      </div>
    </div>
  </nav>

  <!-- Page Layout -->
  <div class="dashboard-container">
    
    <!-- Sidebar -->
    <aside class="sidebar">
      <ul class="sidebar-menu">
        
  <li><a class="nav-link active" href="/hotelDash"> 🏢 Hotels</a></li>
	<li><a class="nav-link active" href="/cityDash">🌆 City</a></li>
	<li><a class="nav-link active" href="/areaDash">📍Area</a></li>
  <li><a class="nav-link active" href="/amenitiesDash">🛎️ Amenities</a></li>
	<li><a class="nav-link active" href="viewcustomer">👥 Customer</a></li>
	<li><a class="nav-link active" href="viewReview"> ⭐ Rating & Reviews</a></li>
   <li><a class="nav-link" href="/bookingDetails">📚 Bookings</a></li>
  <li><a class="nav-link" href="/logout-form">🚪 Logout</a></li>
      </ul>
    </aside>

  <main class="main-body">
    
    <div class="iframe-wrapper">
      <iframe id="contentFrame" src="" scrolling="no"></iframe>
    </div>
   </main>

  </div>

  <!-- JavaScript for Profile Dropdown -->
  <script>
    function toggleProfileMenu() {
      const menu = document.getElementById("profileMenu");
      menu.style.display = menu.style.display === "block" ? "none" : "block";
    }

    // Hide menu on outside click
    document.addEventListener("click", function(event) {
      const profileContainer = document.querySelector(".profile-container");
      const profileMenu = document.getElementById("profileMenu");
      if (!profileContainer.contains(event.target)) {
        profileMenu.style.display = "none";
      }
    });


	function loadPage(page, clickedLink) {
      		document.getElementById('contentFrame').src = page;

      // Handle active class
      document.querySelectorAll('.nav-link').forEach(link => {
        link.classList.remove('active');
      });
      clickedLink.classList.add('active');
    }

  </script>
</body>
</html>