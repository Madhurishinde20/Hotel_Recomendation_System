<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Login</title>
  <style>
   body {
  margin: 0;
  padding: 0;
  font-family: Arial, sans-serif;
  background-image: url('/image/h6.jpg');
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
  background-attachment: fixed;
}
    .loginform {
      margin: 20px auto;
      padding: 40px;
      width: 80%;
      max-width: 600px;
      border-radius: 10px;
    }

    form {
  width: 100%;
  margin: 0 auto;
  padding: 30px;
  background-color: rgba(255, 255, 255, 0.8); /* This makes the form background semi-transparent */
  border: 1px solid lightgrey;
  border-radius: 10px;
  box-shadow: 0 0 15px rgba(255, 182, 193, 0.4);
}


    form h1 {
      text-align: center;
      margin-bottom: 20px;
      color: #333;
    }

    form input[type="text"],
    form input[type="password"],
    form input[type="submit"],
    select.form-control {
      width: 100%;
      padding: 10px;
      font-size: 16px;
      border-radius: 5px;
      border: 1px solid #999;
      margin-top: 10px;
      box-sizing: border-box;
    }

    form input[type="submit"] {
      background-color: purple;
      font-weight: bold;
      cursor: pointer;
      color: white;
      transition: background-color 0.3s ease;
    }

    form input[type="submit"]:hover {
      background-color: #d2b48c;
    }

    /* Styled select to match other inputs */
    select.form-control {
      background-color: white;
      appearance: none;
      -webkit-appearance: none;
      -moz-appearance: none;
      background-image: url("data:image/svg+xml;utf8,<svg fill='gray' height='24' viewBox='0 0 24 24' width='24' xmlns='http://www.w3.org/2000/svg'><path d='M7 10l5 5 5-5z'/></svg>");
      background-repeat: no-repeat;
      background-position: right 10px center;
      background-size: 20px;
    }

    select.form-control:focus {
      outline: none;
      border-color: cornflowerblue;
      box-shadow: 0 0 5px rgba(100, 149, 237, 0.5);
    }

    .toggle-link {
      text-align: center;
      display: block;
      margin-top: 15px;
      color: black;
      font-weight: bold;
    }

    .toggle-link a {
      color: blue;
      text-decoration: none;
    }

    .toggle-link a:hover {
      text-decoration: underline;
    }

    @media (max-width: 480px) {
  body {
    padding: 10px;
    background-attachment: scroll; /* Prevent fixed background from causing jitter */
  }

  .loginform {
    width: 100%;
    padding: 10px;
    margin: 10px auto;
    box-shadow: none;
    border-radius: 0;
  }

  form {
    padding: 15px;
    border-radius: 5px;
  }

  form h1 {
    font-size: 22px;
    margin-bottom: 15px;
  }

  form input[type="text"],
  form input[type="password"],
  form input[type="submit"],
  select.form-control {
    width: 100%;
    font-size: 15px;
    padding: 10px;
  }

  form input[type="submit"] {
    padding: 12px;
    font-size: 16px;
  }

  .toggle-link {
    font-size: 14px;
    margin-top: 10px;
  }

  .toggle-link a {
    font-size: 14px;
  }
}

  </style>

  <!-- Optional: link your JS if used -->
  <script src="/validation.js" type="text/javascript"></script>
</head>
<body>
  <div class="loginform">
    <form name="frm" action="/validate" method="POST" onsubmit="return loginForm()">
      <h1>Login</h1>

      <input type="text" name="username" id="username" placeholder="Enter Username" oninput="validationName(this.value)">
      <span id="nmsg"></span><br><br>

      <input type="text" name="password" id="password" placeholder="Enter Password" oninput="validatePassword(this.value)">
      <span id="pmsg"></span><br><br>

      <select name="type" class="form-control">
        <option value="" disabled selected hidden>Select Role</option>
        <option value="Admin">Admin</option>
        <option value="User">User</option>
      </select><br><br>

      <input type="submit" name="s" value="Login"><br><br>

      <p class="toggle-link">Don't have an account? <a href="/signup">Register</a></p>

      <span id="h"></span>
    </form>
  </div>
</body>
</html>
